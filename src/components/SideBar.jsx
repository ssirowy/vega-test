import styled from 'styled-components'

export const SideBar = styled.div`
         grid-area: side;
         border-left: 2px solid #dadada;
         overflow-y: auto;
         padding: 16px;
       `;